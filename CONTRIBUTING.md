# Contribution Guidelines

What is needed for now is just the raw HTML5 structure of the document.. Contributors should modify **only** the index.html file and write the whole document. No need to modify anything outside &lt;body&gt; tag.

After this, then we'll start working on our stylesheet.
